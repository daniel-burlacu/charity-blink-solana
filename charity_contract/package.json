{
  "license": "ISC",
  "scripts": {
    "create": "npm run clean && npm run build && npm run anchor-build && npm run deploy",
    "test": "anchor test --skip-deploy",
    "clean": "cargo clean",
    "build": "cargo build",
    "anchor-build": "anchor build",
    "deploy": "anchor deploy",
    "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/wallet-adapter-react": "^0.15.35",
    "luxon": "^3.5.0",
    "yarn": "^1.22.22"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
